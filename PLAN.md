# 🗺️ План розширення функціоналу Mr.Snoopy Grooming Bot

> Документ описує **що** треба реалізувати, **як** саме, і **в якому порядку**.
> Статуси: ⬜ не почато · 🟨 в роботі · ✅ готово

---

## 0. Поточний стан і ключове рішення

Зараз бот — "тонкий" AI-консультант:
- `webhook_bot.py` — Flask + webhook, event loop у фоновому потоці
- `groq_client.py` — Groq API, історія чату **in-memory** (зникає при рестарті)
- Жодної бази даних, кнопок, фонових задач

**Ключове рішення: салон вже працює на Altegio** (alteg.io) — там живуть записи, розклад, послуги, ціни, клієнтська база і програма лояльності.

Тому архітектурний принцип усього плану:

> **Altegio — єдине джерело правди** для записів, слотів, послуг, цін, клієнтів і бонусів.
> **Бот — Telegram-інтерфейс до Altegio** + власна маленька БД лише для того, чого в Altegio немає.

Що це знімає з плану:
- ❌ Власні таблиці слотів/послуг/цін/записів — беремо з Altegio API
- ❌ Власна бонусна система — Altegio має loyalty API
- ❌ Питання "яка CRM?" — Altegio і є CRM, клієнти створюються прямо там
- ❌ Адмін-механіка генерації слотів — адміністратор веде розклад в Altegio як звик

Що все одно потрібно своє:
- ✅ Маленька БД (Supabase, безкоштовний tier): зв'язка Telegram ↔ Altegio, картки улюбленців, черга сповіщень, оцінки, фото до/після, історія AI-чату
- ✅ Планувальник (cron) для Telegram-нагадувань (Altegio шле SMS/push у своєму додатку, але не в наш Telegram-бот)
- ✅ Приймання вебхуків від Altegio (запис створили/перенесли/скасували на сайті чи адміністратором → бот одразу в курсі)

---

## Altegio API — що використовуємо

Документація: https://developer.alteg.io/en

| Потреба бота | Механізм Altegio |
|---|---|
| Вільні слоти, дати, майстри | Online Booking API: `book_dates`, `book_times`, `book_staff`, `book_services` (публічні, без user token) |
| Створити запис | Online Booking API: `book_record` |
| Мої записи / перенести / скасувати | Business API: records клієнта (get / update / delete) |
| Клієнти: створити, знайти по телефону, оновити | Business API: clients (+ custom fields) |
| Послуги та ціни | Business API / Online Booking: services з цінами |
| Локації (філії) | companies/locations |
| Бонуси | Loyalty API: баланс, транзакції |
| Події "запис створено/змінено/видалено" | WebHooks (Settings → System settings → WebHook → наш endpoint) |
| Візит відбувся (attendance) | статус record-а в Altegio (адмін відмічає у своєму інтерфейсі як звик) |

**Авторизація:** `Authorization: Bearer <partner_token>, User <user_token>`. Отримати:
1. Partner token (доступ до API / маркетплейс Altegio)
2. User token користувача з потрібними правами
3. `company_id` кожної філії

### ⚠️ Перевірити на старті (Фаза 0, до написання коду)
1. **Тариф/доступ до API** — чи відкритий API на поточному тарифі салону.
2. **Улюбленці в Altegio** — чи доступні картки тварин через API (в документації явно не описано). Якщо ні → зберігаємо улюбленців у своїй БД, а в Altegio-записі передаємо ім'я/породу в коментарі.
3. **Loyalty API** — чи використовує салон бонусну програму Altegio і чи читається баланс по API. Якщо ні → власні таблиці бонусів (запасний варіант описаний у Фазі 6).
4. **Правило 24 год** — в Altegio є власне налаштування вікна онлайн-скасування; узгодити, щоб бот і сайт поводились однаково.

---

## 💸 Бюджет: всі інструменти безоплатні

| Інструмент | Тариф | Обмеження / нюанси |
|---|---|---|
| Telegram Bot API | безкоштовно | — |
| Groq API | free tier | rate limits (вистачає для салону); вже використовується |
| PythonAnywhere | free tier | 1 web app; **вихідний інтернет лише через HTTPS-проксі з білим списком** (див. ризик №5); 1 щоденний scheduled task |
| Supabase | free tier | Postgres 500 MB — вистачить на роки; проєкт «засинає» після 7 днів без запитів (живий бот + cron цього не допустять) |
| cron-job.org | безкоштовно | зовнішній cron кожні 10–15 хв на наш `/cron` endpoint — обходить обмеження PythonAnywhere на scheduled tasks |
| Altegio API | у складі підписки салону | салон вже платить за Altegio; доступ до API перевірити (ризик №1) |

Нічого докуповувати не треба. Якщо колись впремось у ліміти PythonAnywhere — безкоштовні альтернативи для Flask-webhook: Render free tier, Railway trial, Fly.io.

---

## Архітектурні рішення

### БД: Supabase (безкоштовний tier)
- **Supabase** = хмарний Postgres + веб-панель. Нічого не треба хостити самим; адміністратор може дивитись дані (улюбленці, оцінки) прямо в браузері.
- Підключення — **тільки через `supabase-py` (REST, HTTPS)**, НЕ напряму до Postgres: безкоштовний PythonAnywhere блокує сирі TCP-з'єднання (порт 5432), а HTTPS через проксі працює.
- Схему таблиць створюємо через SQL Editor у панелі Supabase (SQL-скрипт тримаємо в репо: `db/schema.sql`), тому SQLAlchemy/Alembic не потрібні.
- Ключі: `SUPABASE_URL` + `SUPABASE_SERVICE_KEY` у `.env` (service key, бо звертається тільки наш бекенд; Row Level Security можна не вмикати).
- Зберігає ТІЛЬКИ те, чого немає в Altegio (модель нижче).

### Scheduler: PythonAnywhere Scheduled Task → HTTP endpoint
- Endpoint `POST /cron/<CRON_SECRET>` у Flask, викликається кожні 10–15 хв (PythonAnywhere Scheduled Task на платному тарифі або безкоштовний зовнішній cron типу cron-job.org).
- Всі заплановані сповіщення — в таблиці `notifications` (`pending`/`sent`), рестарти нічого не гублять.

### Вебхуки Altegio: endpoint `POST /altegio/webhook`
- Приймає події record created/updated/deleted.
- Синхронізує стан: перепланувати нагадування при переносі, скасувати їх при видаленні запису, надіслати клієнту підтвердження якщо запис створив адміністратор по телефону.
- Перевірка автентичності (секрет в URL + звірка даних через API).

### UI бота: Inline-кнопки + ConversationHandler
- Головне меню — `ReplyKeyboardMarkup`; флоу (запис, анкета, оцінка) — `InlineKeyboardMarkup` + `CallbackQueryHandler` + `ConversationHandler`.
- Стан ConversationHandler in-memory; чернетка анкети додатково в БД (`draft_json`), щоб рестарт не втрачав прогрес.

### Ролі
- `ADMIN_GROUP_CHAT_ID` + `ADMIN_TOPIC_ID` у `.env` — усі сповіщення (низька оцінка, незаповнена анкета, "потрібна допомога") летять одним повідомленням у топік адмін-групи.
- Адмін-функції в боті — мінімальні (фото до/після, рекомендації майстра). Все інше адміністратор робить в Altegio.

### Нова структура файлів

```
grooming-telegram-bot/
├── webhook_bot.py          # Flask: telegram webhook + /cron + /altegio/webhook (тонкий)
├── bot.py                  # polling для локальної розробки
├── config.py               # + ALTEGIO_*, ADMIN_GROUP_CHAT_ID/ADMIN_TOPIC_ID, CRON_SECRET, GOOGLE_MAPS_REVIEW_URL
├── groq_client.py          # AI-помічник (+ function calling поверх Altegio)
├── db/
│   ├── schema.sql          # SQL-схема таблиць (виконується в Supabase SQL Editor)
│   └── client.py           # supabase-py клієнт + функції доступу до даних
├── services/
│   ├── altegio.py          # ЄДИНИЙ клієнт Altegio API (slots, records, clients, loyalty)
│   ├── visit_history.py    # минулі візити клієнта з Altegio (по всіх філіях)
│   ├── scheduler.py        # диспетчер cron-подій
│   └── notifications.py    # створення/відправка сповіщень
├── handlers/
│   ├── menu.py             # головне меню, /start
│   ├── registration.py     # анкета: телефон → клієнт в Altegio + улюбленці
│   ├── booking.py          # запис через Altegio Online Booking API
│   ├── my_bookings.py      # перегляд / перенос / скасування / повтор
│   ├── pets.py             # картки улюбленців
│   ├── history.py          # історія візитів (records з Altegio) — фото/рекомендації не робимо, див. Фазу 5
│   ├── bonuses.py          # баланс з Altegio Loyalty
│   ├── feedback.py         # оцінки після візиту
│   ├── help_admin.py       # "Потрібна допомога" → менеджеру
│   └── admin.py            # ❌ не робимо: персонал працює в Altegio, не в боті (див. Фазу 5)
```

---

## Модель даних у Supabase (мінімальна)

```
clients        id, tg_user_id (unique), altegio_client_id, phone, name,
               registration_done (bool), draft_json (чернетка анкети),
               last_promo_at (anti-spam)

pets           id, client_id → clients, name, breed, birth_date, weight,
               allergies, behavior_notes, photo_file_id,
               rabies_vaccine_date, vaccine_photo_file_ids (json),
               altegio_pet_id (nullable — якщо Altegio віддає тварин по API)

tracked_records id, altegio_record_id (unique), client_id, pet_id,
               starts_at, service_title, location_title, status,
               raw_json (останній стан з Altegio/вебхука)
               -- локальний "кеш" записів: щоб планувати нагадування
               -- і зв'язувати запис ↔ улюбленець ↔ оцінка

visit_extras   id, altegio_record_id, groomer_recommendations (text),
               next_visit_weeks (int), photo_before_ids (json), photo_after_ids (json)
               -- те, чого в Altegio немає: фото до/після + рекомендації майстра

ratings        id, altegio_record_id, service_stars (1–5), groomer_stars (1–5),
               comment, created_at

notifications  id, client_id, type (reminder_2h / thanks_rating / rebook_nudge /
               birthday / vaccine / form_incomplete / custom),
               payload_json, send_after, status (pending/sent/failed), sent_at

chat_messages  id, client_id, role, content, created_at
               -- історія AI-чату (переживає рестарти, обрізаємо до ~20 останніх)
```

Telegram-фото зберігаємо як `file_id` (Telegram тримає файли вічно).
Записи, слоти, послуги, ціни, бонуси — **НЕ дублюємо**, читаємо з Altegio (крім легкого кешу `tracked_records` для нагадувань).

---

## Фази реалізації

### ✅ Фаза 0 — Фундамент + інтеграція з Altegio

**Що зробити:**
1. **Отримати доступи Altegio**: partner token, user token, company_id філій. Перевірити 4 питання з блоку "Перевірити на старті".
2. `services/altegio.py` — клієнт API: `get_locations()`, `get_services()`, `get_free_dates/times()`, `create_record()`, `get_client_records()`, `move_record()`, `cancel_record()`, `find_client_by_phone()`, `create_client()`, `get_loyalty_balance()`. Обробка помилок і rate limits в одному місці.
3. **Supabase**: створити безкоштовний проєкт → виконати `db/schema.sql` у SQL Editor → залежність `supabase` у `requirements.txt` → `db/client.py` з функціями доступу. Перевірити з PythonAnywhere, що REST-запити до `*.supabase.co` проходять (див. ризик №5).
4. `config.py`: + `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `ALTEGIO_PARTNER_TOKEN`, `ALTEGIO_USER_TOKEN`, `ALTEGIO_COMPANY_IDS`, `ADMIN_GROUP_CHAT_ID`, `ADMIN_TOPIC_ID`, `CRON_SECRET`, `GOOGLE_MAPS_REVIEW_URL`.
5. Endpoint `POST /cron/<CRON_SECRET>` → `services/scheduler.run_due()`.
6. Endpoint `POST /altegio/webhook` → обробка record created/updated/deleted → оновлення `tracked_records` + перепланування нагадувань. Налаштувати вебхук в Altegio (Settings → System settings → WebHook).
7. Налаштувати cron (PythonAnywhere Scheduled Task або cron-job.org) кожні 10–15 хв.
8. Головне меню: `📅 Записатись` · `🗓 Мої записи` · `🐾 Мої улюбленці` · `✂️ Історія` · `🎁 Бонуси` · `💬 Питання (AI)` · `🆘 Допомога`.
9. Роутинг: кнопки меню → handler-и; інший текст → Groq (як зараз).

**Критерій готовності:** тестовий скрипт через `services/altegio.py` дістає реальні послуги/слоти салону; вебхук від Altegio долітає і логується; меню працює; AI-чат не зламаний.

---

### ✅ Фаза 1 — Реєстрація клієнта + картки улюбленців

**Зроблено (код у master, коміти `e4f6b38`, `2215e9c`):**
- ✅ Анкета (ConversationHandler): телефон → вибір локації → ім'я → цикл улюбленців (ім'я, порода, дата народження, вага, алергії, поведінка, фото — **усі поля обов'язкові**, кнопку «Пропустити» прибрано). Прогрес пишеться в БД одразу — рестарт не втрачає заповнене; `/cancel` — вихід
- ✅ Питання про вакцинацію прибрано з анкети (дату веде адмін у картці Altegio, див. Фазу 10)
- ✅ Прив'язка до Altegio: пошук по телефону по всіх філіях (обрана клієнтом — першою); давнього клієнта вітаємо по імені з Altegio без дубліката і без питання «як вас звати»; не знайдено → створення в обраній філії; при помилці пошуку клієнт НЕ створюється (захист бази від дублікатів)
- ✅ Синхронізація улюбленців у коментар клієнта Altegio зі збереженням нотаток адміністратора (оновлюється лише блок після маркера `=== Улюбленці (Telegram-бот) ===`); коректно очищує блок, якщо улюбленців не лишилось. Виправлено 422 від Altegio: `PUT client` вимагає `phone` навіть коли міняється лише коментар
- ✅ Картки улюбленців: список → картка з фото → редагування поля за полем з валідацією + перевірка власності (захист від IDOR через підроблену callback data)
- ✅ Додавання ще одного улюбленця після завершення анкети — кнопка «➕ Додати улюбленця» у списку улюбленців (раніше цей флоу існував лише всередині первинної анкети й був недоступний після її завершення)
- ✅ Видалення улюбленця — кнопка «🗑 Видалити» в картці з підтвердженням; видаляється з Supabase, коментар клієнта в Altegio синхронізується
- ✅ Сповіщення адмінам: перевірка о 21:00 (Київ) «не заповнив анкету» (cron + таблиця `notifications`). Миттєвий пуш «🆕 Новий користувач» на кожен `/start` прибрано (2026-07-20) — при великій кількості клієнтів він забивав чат і топив під собою важливі сповіщення про незавершену реєстрацію
- ✅ Рефакторинг: єдина реєстрація handler-ів у `handlers/setup.py` для webhook_bot.py і bot.py

**Лишилось до ✅:**
- ✅ Виконано `db/migrations/001_phase1_altegio_company_id.sql` у Supabase SQL Editor (колонка `altegio_company_id` у `clients`)
- ✅ `ADMIN_GROUP_CHAT_ID` + `ADMIN_TOPIC_ID` у `.env` на PythonAnywhere → Reload web app (лог чистий, бот стартував без помилок)
- ✅ cron-job.org налаштовано: job "Mr.Snoopy Bot - scheduler dispatch" кожні 15 хв, POST на `/cron/<CRON_SECRET>`, тестовий запуск підтвердив 200 OK від продакшена
- ✅ Живий прогін критерію готовності (2026-07-20): новий клієнт пройшов анкету від початку до кінця, створений у Altegio; давній клієнт (той самий телефон, чистий Supabase) знайдений по телефону без дубліката, ім'я підтягнуте автоматично; улюбленець коректно потрапив і в Supabase, і в коментар клієнта Altegio. Тестові дані (Supabase `clients`/`pets` + Altegio-клієнт) прибрані після перевірки.

**Флоу анкети (ConversationHandler):**
1. `/start` → якщо tg_user_id немає в БД → "Давайте знайомитись!" → ім'я → телефон (кнопка `request_contact`).
2. По телефону шукаємо клієнта в Altegio (`find_client_by_phone`): знайшли → прив'язуємо `altegio_client_id`; ні → `create_client()` в Altegio. **Клієнт одразу в CRM** — окрема синхронізація не потрібна.
3. Цикл додавання улюбленця: ім'я → порода → дата народження → вага → алергії → особливості поведінки → фото улюбленця — **усі поля обов'язкові**. Дублюємо ключове (порода, вага, особливості) в коментар/custom fields клієнта в Altegio — щоб адміністратор бачив у своєму інтерфейсі. **Вакцинацію в анкеті не питаємо:** дату веде адміністратор у картці клієнта Altegio, окрема автоматизація перевіряє відсутність/прострочення і сповіщає адміна.
4. "Додати ще одного улюбленця?" → так/ні. Кожен крок зберігає чернетку в `draft_json`.

**Картки улюбленців (`🐾 Мої улюбленці`):** список (+ кнопка `➕ Додати улюбленця` — той самий флоу анкети улюбленця, доступний і після завершення реєстрації) → картка (фото + поля) → `✏️ Редагувати` поле за полем або `🗑 Видалити` з підтвердженням (зміни синхронізуємо в Altegio).

**Контроль заповнення (для адміністратора):**
- Сценарій "адмін дає посилання на бота перед першим візитом": при першому `/start` створюється notification `form_incomplete` з `send_after = сьогодні 21:00`; cron: якщо `registration_done == False` → повідомлення адмінам "Клієнт X не заповнив анкету".

**Критерій готовності:** новий користувач проходить анкету, клієнт з'являється в Altegio (видно в інтерфейсі салону), улюбленці в локальній БД, адмін отримує сповіщення про незаповнені анкети.

---

### ✅ Фаза 2 — Онлайн-запис на грумінг ⭐ головна функція

**Все через Altegio Online Booking API — ніяких власних слотів.**

**Зроблено (код у master):**
- ✅ `services/altegio.py`: фікс бага `staff_id=0` (трактувався як falsy) в `get_available_dates`; `service_ids` у `get_available_times` (без нього — не ті слоти); `get_service_categories()`; `get_staff()` з опційним фільтром по `service_ids`; `find_available_staff_for_slot()` — резолвить «будь-якого майстра» у конкретного `staff_id` на підтвердженні запису
- ✅ `db/client.py`: `get_client_by_phone()` (матчинг вебхук-записів), `get_tracked_record()`/`upsert_tracked_record()`/`update_tracked_record_status()`
- ✅ `handlers/booking.py`: повний inline-флоу запису та `💰 Дізнатись вартість` — улюбленець → локація (з адресою + гео-піном) → категорія (14 категорій = тип послуги × рівень грумера, окремого кроку «оберіть майстра» нема) → послуга (підказки по породі/вазі улюбленця з фолбеком на повний список з пагінацією) → дата → час → підтвердження → `create_record()` + `tracked_records`
- ✅ Меню: нова кнопка `💰 Дізнатись вартість`, `📅 Записатись` знята з заглушок
- ✅ `services/altegio_webhook.py`: захисний парсинг подій Altegio (форма payload не підтверджена документацією) — upsert `tracked_records`, підтвердження в Telegram для записів, зроблених НЕ через бота (знайдено клієнта по телефону), позначення `cancelled` при видаленні
- ✅ `webhook_bot.py`: роут `/altegio/webhook/<ALTEGIO_WEBHOOK_SECRET>` замість повністю відкритого `/altegio/webhook`
- ✅ Живі перевірки Altegio API (категорії, послуги, дати, часи, `get_company`, резолвинг майстра) — відповідають очікуваній формі
- ✅ `handlers/booking.py`: симетричний (в обидва боки) fallback за породою на кроці вибору послуги — якщо порода не знайдена серед послуг обраного рівня грумера, бот шукає її серед послуг-«сусідів» (той самий тип послуги на інших рівнях: Грумер/PRO/Топ). Знайдено на іншому рівні → кнопки переходу на ці рівні (`bk_switch_cat`); не знайдено на жодному рівні → лише 3 послуги «Інші породи» за вагою + повний прайс + заглушка `🆘 Допомога` (`bk_contact_admin`, майбутнє: клікабельний номер телефону салону). Живий тест (2026-07-21): тестовий улюбленець з породою «Бішон» — підказки по сусідніх рівнях; та сама порода тимчасово замінена на вигадану «Кряквозавр» (перевірено скриптом — 0 збігів з 327 послуг) — підтверджено повний fallback (тільки «Інші породи» + прайс + Допомога), після тесту порода повернута назад на «Бішон»

**Лишилось до ✅:**
- ✅ `ALTEGIO_WEBHOOK_SECRET` у `.env` (локально) і в PythonAnywhere env
- ✅ Вебхук в Altegio налаштовано (`Настройки → Для розробників → Настройки WebHook`, `/settings/web_hook/783219/` — філія Замарстинівська): адреса `/altegio/webhook/<secret>`, подія «Записи» увімкнена
- ✅ Той самий вебхук налаштовано для інших філій (Тернопільська `748415`, Володимира Великого `1364451`) — кожна філія налаштовується окремо
- ✅ PythonAnywhere: Reload web app, підхопив новий `ALTEGIO_WEBHOOK_SECRET` з env
- ✅ Живий тест запису (2026-07-20): клієнт András записався через бота на Замарстинівську (запис `661744900`, 01.08 10:00, «Бішон Фрізе від 5 кг»). Перша спроба впала з 422 — `create_record` не передавав обов'язковий `seance_length`; знайдено, що ця тривалість приходить лише зі слоту `book_times` (у `book_services` і `book_staff` її нема), виправлено (`find_available_staff_for_slot` тепер повертає `(staff_id, seance_length)`). Друга спроба успішна, підтверджено звіркою Supabase `tracked_records` ↔ Altegio `get_client_records` — збігаються повністю
- ✅ Реальна форма payload Altegio-вебхука перевірена в логах (2026-07-20): і `create`, і `delete` події для запису `661744900` мають точно очікувану форму `{"company_id", "resource": "record", "resource_id", "status", "data": {...}}` — парсинг у `services/altegio_webhook.py` збігається без змін. Скасування тестового запису клієнтом в Altegio → вебхук `status: "delete"` → `tracked_records.status` автоматично став `cancelled`, без помилок у логах і без дублю пушу в Telegram (запис уже був у БД — за дизайном тихо оновлюється)

**⚠️ Оновлення (2026-07-27) — редизайн на зовнішній віджет:**
- `📅 Записатись` (меню) більше НЕ веде у внутрішній inline-флоу вибору дати/часу/майстра — одразу дає пряме посилання на зовнішній Altegio-віджет (`config.ALTEGIO_BOOKING_WIDGET_URL = https://n1358931.alteg.io/`, один лінк на всі 3 філії; клієнт сам обирає філію/послугу/дату/час/майстра всередині віджету).
- `💰 Дізнатись вартість` лишився внутрішнім (крок категорії/послуги з підказками по породі/вазі — без змін), але крок вибору локації прибрано: ціни рахуються одразу для «домашньої» філії клієнта (`clients.altegio_company_id`, обраної при реєстрації); після вибору послуги замість підтвердження запису — та сама кнопка «📅 Записатись» на зовнішній віджет.
- `book_start()`/`_select_service()` (обидва — момент показу лінка на віджет) плановують `booking_incomplete` (сповіщення адмінам о 18:00, якщо запис так і не з'явився) — тобто відстеження завжди стартує рівно в момент видачі лінка на віджет, незалежно від того, яким з двох шляхів клієнт туди прийшов.
- Внутрішній `create_record()`-флоу (кроки локація → дата → час → підтвердження) видалено разом із `_ask_location`/`_location_keyboard`/`_show_location_card` як мертвий код.
- Наслідок: коментар до запису з даними улюбленця (п.5 нижче) більше НЕ пишеться для записів через віджет — це прийнятний компроміс (майстер бачить менше контексту, ніж раніше через бота, але клієнт має повний контроль вибору майстра/часу в самому Altegio).

**Флоу «💰 Дізнатись вартість» (актуальний):**
1. Вибір улюбленця (якщо один — автоматично).
2. Категорія послуг (14 = тип × рівень грумера) → послуга з ціною, підказки по породі/вазі улюбленця.
3. Картка "🐶 Барні · Комплекс · 📍 Локація · 💰 800 грн" → кнопка «📅 Записатись» (зовнішній віджет).

**Записи, зроблені НЕ через бота** (по телефону, на сайті, через зовнішній віджет): прилітають вебхуком → якщо клієнт відомий по телефону → додаємо в `tracked_records`, шлемо підтвердження в Telegram і теж плануємо нагадування. Це великий плюс: бот покриває нагадуваннями ВСІ записи салону, не тільки свої.

**Критерій готовності:** запис із бота з'являється в календарі Altegio; запис, створений адміністратором в Altegio, з'являється у клієнта в боті.

---

### ✅ Фаза 3 — Управління записами

**Зроблено (код у master):**
- ✅ `db/migrations/002_phase3_tracked_records_slot_fields.sql` — нові колонки `tracked_records.company_id`/`altegio_service_id` (потрібні для `move_record`/`cancel_record`, яких за самим `raw_json` надійно не витягнути — форма вебхук-payload не гарантована)
- ✅ `db/client.py`: `get_tracked_record_by_id()`, `get_upcoming_tracked_records()`, `get_last_past_tracked_record()`
- ✅ `handlers/common.py`: спільні `format_date_label()`/`UA_WEEKDAYS`/`parse_iso_datetime()` (перенесено з `booking.py`, щоб не дублювати в `my_bookings.py`)
- ✅ `handlers/booking.py`: `_confirm_booking` записує `company_id`/`altegio_service_id` в `tracked_records`; новий публічний вхід `start_from_pet_and_service()` для повтору запису з іншого модуля
- ✅ `services/altegio_webhook.py`: ті самі `company_id`/`altegio_service_id` тепер зберігаються і для записів, зроблених поза ботом
- ✅ `handlers/my_bookings.py` (новий файл): `🗓 Мої записи` — картка на кожен майбутній запис (`🔁 Перенести`/`❌ Скасувати`, якщо > 24 год до візиту; інакше текст із проханням звернутись до адміністратора) + `🔄 Повторити останній запис`, якщо є минулий не скасований візит
- ✅ `handlers/menu.py`/`handlers/setup.py`: кнопка знята із заглушок, `mb_*` callback-и підключені

**Лишилось до ✅:**
- ✅ Виконано `002_phase3_tracked_records_slot_fields.sql` у Supabase SQL Editor (2026-07-21) — колонки `company_id`/`altegio_service_id` підтверджені в `tracked_records`
- ✅ Живий тест переносу/скасування на локальному боті (`bot.py`, polling) — `move_record`/`cancel_record` це прямі вихідні виклики Altegio API, ідентичні незалежно від того, чи бот отримує апдейти через polling чи через прод-вебхук, тож окремий тест у проді не потрібен
- ✅ Живий тест «Повторити останній запис» (2026-07-21): синтетичний минулий запис у `tracked_records` для тестового улюбленця → `mb_repeat` коректно підхопив улюбленця/послугу і повів одразу на вибір дати → новий запис успішно створився в Altegio, після тесту скасований; тестовий рядок прибрано з `tracked_records`

**⚠️ Оновлення (2026-07-27) — збереження майстра (`staff_id`):**
- `db/migrations/006_tracked_records_staff_id.sql` — нова колонка `tracked_records.staff_id`; `services/altegio_webhook.py` тепер витягує `staff_id` з payload запису (форма поки не підтверджена офіційно — захисний `.get()`) і зберігає його при кожному upsert.
- `🔁 Перенести` і `🔄 Повторити останній запис` більше не пропонують «будь-якого вільного» майстра — якщо `tracked_records.staff_id` минулого запису відомий, `book_dates`/`book_times` запитуються саме для нього; клієнту показується ім'я майстра (`booking.staff_name()`). Немає збереженого `staff_id` (старі записи до міграції) → фолбек на попередню поведінку («будь-який вільний», `find_available_staff_for_slot()`).
- `handlers/booking.py::_confirm_booking` і `handlers/my_bookings.py::_confirm_reschedule` пишуть `staff_id` у `tracked_records` при кожному успішному записі/переносі — ланцюжок самопідтримний: раз зафіксований майстер лишається «прилиплим» до наступних повторів/переносів того ж клієнта.

**Флоу (`🗓 Мої записи`):**
1. Список майбутніх активних записів з `tracked_records` (без live-звірки з Altegio на кожен перегляд — вебхуки й так тримають кеш синхронним; самі дії перенос/скасування завжди йдуть напряму в Altegio).
2. `🔁 Перенести` — якщо до візиту **> 24 год**: вибір нової дати/часу для того самого майстра, якщо відомий (`book_dates`/`book_times` для тієї ж послуги) → `move_record()`. Інакше: текст "Перенести можна не пізніше ніж за 24 години — зверніться до адміністратора через «🆘 Допомога»".
3. `❌ Скасувати` — те саме правило 24 год + підтвердження → `cancel_record()` → `tracked_records.status = cancelled` → сповіщення адмінам (`notifications.notify_admins_async`, перше реальне використання цієї функції).
4. `🔄 Повторити останній запис` — останній не скасований минулий запис (улюбленець + послуга + локація + майстер з `tracked_records`) → одразу `booking.start_from_pet_and_service()` на крок вибору дати.

**Критерій готовності:** перенос/скасування відображаються в календарі Altegio миттєво; правило 24 год працює; повтор — 2–3 натискання.

---

### ✅ Фаза 4 — Автоматичні нагадування + оцінки

Через `notifications` + cron + вебхуки Altegio.

**✅ Частина 1 (реалізовано) — reminder_2h + thanks_rating:**
- Без окремого поллінгу статусу відвідування: Altegio вже віддає час початку і тривалість (`seance_length`, секунди) при створенні/оновленні запису — час завершення рахується напряму (`ends_at = starts_at + seance_length`), без cron-перевірки attendance.
- При появі/зміні запису в `tracked_records` (з бота — `handlers/booking.py::_confirm_booking`, перенос — `handlers/my_bookings.py::_confirm_reschedule`, АБО вебхуком Altegio — `services/altegio_webhook.py`) плануються:
  - `reminder_2h`: `send_after = starts_at − 2.5 год` — "Нагадуємо: сьогодні о 14:00 чекаємо Барні 🐾 …".
  - `thanks_rating`: `send_after = ends_at + 45 хв` — "Дякуємо, що завітали до Mr.Snoopy Grooming! 💛" (поки без оціночного флоу, див. нижче).
- `services/notifications.py::schedule_visit_notifications()` — ідемпотентне (пере)планування: завжди спершу чистить старі pending-нагадування запису (`altegio_record_id` на `notifications`, міграція `006`), тож перенос і повторні вебхук-оновлення обробляються без дублів. `cancel_visit_notifications()` — при скасуванні (клієнтом або в Altegio).
- Заразом виправлено побічний баг: `starts_at` з вебхука (`data.date`, наївний рядок) не мав явної Kyiv-локалізації — Supabase/PostgREST трактував би його як UTC (зсув на 2-3 год). Тепер `services/altegio_webhook.py::_parse_kyiv()` локалізує явно.
- **Відоме обмеження:** нагадування плануються лише для записів, створених/змінених після деплою — записи, що вже існували в `tracked_records` раніше, нагадувань "заднім числом" не отримають.
- **✅ Баг таймзони виправлено (2026-08-18, знайдений під час Фази 5):** `services/altegio_webhook.py::process_record()` брав час запису як `data.get("datetime") or data.get("date")`, тобто віддавав перевагу полю `datetime`. Але Altegio ставить у `datetime` offset `+03:00` **незалежно від сезону** (перевірено на живих записах січня 2026), тож для візитів у зимовий час (Київ у `+02:00`) `tracked_records.starts_at` виходив на годину раніше за реальний: `reminder_2h`/`thanks_rating` спрацювали б завчасно, а `🗓 Мої записи` показував би не той час. Тепер наївний `date` (справжній час на годиннику салону) читається першим, `datetime` лишається лише фолбеком — так само, як у `services/visit_history.py`. Покрито тестом `services/test_altegio_webhook.py::test_winter_record_keeps_salon_wall_clock_time` (на старому коді падав: `06.01.2026 10:15` замість `11:15`). Баг спав, бо влітку Київ теж у `+03:00` — дедлайном був перехід на зимовий час у кінці жовтня.
  - **Заразом той самий шаблон знайдено ще у двох шляхах створення запису:** `handlers/booking.py::_confirm_booking` і `handlers/nearest_slots.py` писали в `tracked_records.starts_at` значення `record.get("datetime")` з відповіді `create_record()`, а локально порахований київський час був лише фолбеком. Опору на поле Altegio прибрано: тепер завжди пишеться `kyiv_datetime(date_str, time_str)`/`to_kyiv_iso(...)` — той самий час, який обрав клієнт, тож поле Altegio нічого не додавало, а offset ламало. Ці два шляхи юніт-тестами не покриті (флоу підтвердження запису взагалі не має тестового каркаса) — перевірено читанням коду.

**✅ Частина 2 (реалізовано, спрощено 2026-08-03) — флоу оцінки:**
- `thanks_rating` питає одну оцінку — роботу майстра: "Як оцініте роботу майстра?" `⭐1…5` (`services/scheduler.py::_handle_thanks_rating` шле inline-клавіатуру через параметр `reply_markup` у `send_telegram_message`).
- **Початково була реалізована двоступенева версія** (спершу "Оцініть сервіс", потім "Оцініть роботу грумера") — після живого тесту на реальному клієнті з'ясувалось, що друга клавіатура (візуально ідентична першій) виглядає як "бот не зарахував тап і питає знову", плутає клієнтів. Продукту досить оцінки майстра — спрощено до одного питання, один тап (`db.create_rating` тепер приймає `service_stars=None`, колонка в схемі й так nullable).
- Стан флоу — у `callback_data` (`rt_master:<record_id>:<stars>`), без `context.user_data`: клієнт може мати кілька `thanks_rating`-промптів одночасно, і стан, ключований лише по user_id, дав би колізію між ними. Обробник — `handlers/rating.py`, зареєстрований у `handlers/setup.py` (`^rt_`).
- Оцінка пишеться в `ratings` (`db.create_rating`, `service_stars=None`); повторний тап на стару кнопку — ідемпотентно, `db.get_rating` не дає створити дубль чи повторно сповістити адмінів.
- **≤ 3** → миттєво адмінам через `notifications.notify_admins_async`: "⚠️ Низька оцінка від {клієнт} ({телефон}): Майстер: Y★."
- **5** → кнопка "Залишити відгук на Google Maps", посилання — з `config.GOOGLE_MAPS_REVIEW_URLS` (по назві філії з `tracked_records.location_title`, окремий лінк на кожну з 3 філій).
- **Безпека:** `callback_data` (`rt_master`) приходить від Telegram-клієнта і може бути підроблена (кастомний клієнт здатен відправити довільний callback_data на чуже повідомлення) — `handlers/rating.py::_owns_record()` перевіряє, що `update.effective_user.id` збігається з `tg_user_id` клієнта, прив'язаного до запису, перш ніж записати оцінку.
- **Свідомо відкладено (не входило в цю частину):** опційний текстовий коментар після оцінки — вимагав би `context.user_data`-прапорця, перевіреного до catch-all `ai_chat.handle_message`, що вносить крихкість (застарілий/сплутаний стан при паралельних флоу оцінки) непропорційну вузькому запиту.

**✅ Частина 3 (реалізовано) — rebook-нагадування:**
- Після візиту планується `rebook_nudge`: `send_after = ends_at + REBOOK_DEFAULT_WEEKS` (Фаза 5 з рекомендацією майстра ще не реалізована, тож поки фіксований дефолт — 6 тижнів, константа в `services/notifications.py`) — "🐩 {улюбленець} час на грумінг! Записатись?" + кнопка на `config.ALTEGIO_BOOKING_WIDGET_URL`.
- Той самий тип доданий у `_VISIT_NOTIF_TYPES`, тож ідемпотентне (пере)планування/скасування (`schedule_visit_notifications`/`cancel_visit_notifications`) автоматично покриває і його — окремого коду скасування не знадобилось.
- `services/scheduler.py::_handle_rebook_nudge` гасить нагадування мовчки (`"sent"` без відправки), якщо клієнт уже встиг записатись після візиту (`db.has_tracked_record_since`) — той самий хелпер, що й `booking_incomplete`.

**Критерій готовності (повний): ✅ пройдено 2026-08-03.** Живий тест на синтетичних тестових записах у прод-Supabase (без торкання Altegio) через ручний тригер `/cron/<CRON_SECRET>`: `reminder_2h` → `thanks_rating` → оцінка → (5⭐ → кнопка Google Maps; ≤3⭐ → алерт в адмін-групу) → `rebook_nudge` — усі повідомлення прийшли й відпрацювали коректно, спершу на двоступеневому флоу, потім (після спрощення до одного кроку) повторно на András — підтверджено один тап = одна подяка.

Міграція `007_phase4_visit_notifications.sql` підтверджена застосованою в прод-Supabase.

**✅ Частина 4 (реалізовано 2026-08-04) — щоденна звірка з Altegio (страховка від пропущених вебхуків):**
- **Проблема:** вебхуки Altegio (Частина 1) — основний шлях оновлення `tracked_records`, але доставка нічим не гарантована (ні документацією Altegio, ні PythonAnywhere-проксі). Пропущений вебхук про перенос/скасування залишав би застарілий запис у `tracked_records` — і клієнту летіли б `reminder_2h`/`thanks_rating`/`rebook_nudge` на вже неактуальний візит.
- `services/altegio_webhook.py::_handle()` розділено: логіку обробки одного запису винесено в `process_record(data, company_id)` — спільну для вебхука і нової звірки, жодного дублювання коду. Заразом виправлено: новий-для-бота запис, що вже прийшов скасованим (можливо тому, що його власний вебхук про створення теж пропав), більше не шле хибне підтвердження "✅ Вас записано".
- `services/altegio_reconcile.py` (новий) — `reconcile_upcoming_records()`: щодня перебирає всі 3 філії (`config.ALTEGIO_LOCATIONS`), тягне записи Altegio на найближчі 3 дні (`services/altegio.py::get_records()`, той самий ендпоінт що `get_client_records`, без фільтра клієнта) і проганяє кожен через `process_record()`.
- `services/scheduler.py`: `RECONCILE_HOUR = 6` (Київ) — той самий gating-патерн, що вже є для `vaccine_sync` (`cron_state`, позначається виконаним лише при повному успіху). Час обрано свідомо до відкриття філій (10:00) — щоб перший клієнт дня встиг отримати актуальне нагадування, якщо звірка щось виправила.
- **Живий тест (2026-08-04) виявив і закрив важливий пробіл:** створено тестовий запис в Altegio (філія Замарстинівська, клієнт 185154523, запис 664574746), одразу скасовано — `get_records()` **повністю прибирає скасований запис зі списку**, на відміну від вебхука (де скасування приходить із позначкою `status: "delete"`/`attendance: -1`). Пропущене скасування пройшло б повз "перебір того, що повернув Altegio" непоміченим. Виправлено діффом: `db.get_active_tracked_records_in_range(company_id, вікно)` — усе, що в нас `active` у вікні звірки, але Altegio більше не повертає, позначається `cancelled` (`update_tracked_record_status` + `cancel_visit_notifications`).
- Покриті модульними тестами: `services/test_altegio_webhook.py` (виправлення хибного підтвердження), `services/test_altegio_reconcile.py` (дифф "зниклий запис → cancelled" і "присутній запис → без змін").

---

### 🟨 Фаза 5 — Історія грумінгу

**⚠️ Обсяг звужено (2026-08-18):** фото «до/після» і текстові рекомендації майстра з первісного опису фази **не робимо**. Причина не технічна: обидві функції вимагають адмін-інтерфейсу в боті (`/admin` → сьогоднішні візити → надіслати фото/текст), а грумери й адміністратори працюють в Altegio і в бота не заходять — модуль був би написаний і не використовувався. `visit_extras` у `db/schema.sql` лишається створеною, але наразі не використовується.

Якщо колись знадобиться текст рекомендацій — є варіант без нового UI: майстер пише його в коментар запису в Altegio (де вже працює), а бот підтягує, як `services/vaccine_sync.py` уже підтягує дати вакцинації з коментарів клієнта (Фаза 10). Рішення варто приймати тоді, коли буде відповідь на «хто це заповнюватиме».

**Свідомо відкладено окремо:** «наступний візит через N тижнів». Число N не потребує майстра — його можна вивести з історії (середній інтервал між минулими візитами клієнта), і це прибрало б фіксовану константу `REBOOK_DEFAULT_WEEKS = 6`, яка зараз однаково стріляє в `rebook_nudge` (Фаза 4) і в порозі «прострочено» для промо вільних місць (Фаза 7). Рішення поки не прийняте — лишається на потім.

**Зроблено (код у master, PR [#17](https://github.com/Nifargo/MRS_elegram_bot/pull/17)):**
- ✅ `services/visit_history.py` — `get_past_visits(client)`: минулі візити напряму з Altegio (`get_client_records`), найновіші першими. `tracked_records` для історії не годиться — він кешує лише записи бот-епохи і не знає про попередні роки роботи салону (жива перевірка на реальному клієнті дала 12 візитів аж із січня 2025).
  - Повне охоплення філій: у Altegio картка клієнта своя в кожній філії, тому «домашня» береться з `clients.altegio_client_id`, а решта — пошуком по телефону (`find_client_by_phone`). Ціна — до 6 запитів до Altegio на один тап, тож зібраний список кешується в `context.user_data["history"]` на час перегляду.
  - Per-branch try/except (той самий контракт, що `services/rebook_promo.py`): збій однієї філії не приховує візити з решти. Але якщо не відповіла **жодна** — кидаємо `AltegioError`, бо порожній список клієнт прочитав би як «візитів не було».
  - Фільтр «візит відбувся»: відкидаємо видалені записи, явні «не прийшов» (`attendance == -1`) і майбутні. Записи **без** позначки `attendance` лишаються — адміністратор проставляє її не завжди, а приховати через це реальний візит гірше, ніж показати зайвий.
- ✅ `handlers/history.py` — кнопка `✂️ Історія`: сторінки по 5 візитів (дата, послуги, майстер, філія, сума) з навігацією `⬅️ Новіші`/`➡️ Давніші` (`hs_pg:<page>`). ConversationHandler не потрібен — жоден крок не приймає вільного тексту.
  - Улюбленця в картці візиту не показуємо: в Altegio немає зв'язки візит↔тварина (саме тому улюбленці й живуть у Supabase), а назва послуги і так містить породу («Мальтіпу до 4 кг»). Тобто крок «вибір улюбленця» з первісного опису фази фізично не мав би за чим фільтрувати історичні візити.
- ✅ `handlers/menu.py`/`handlers/setup.py`: кнопка знята із заглушок, `hs_*` підключені
- ✅ **Знайдено й обійдено баг таймзони Altegio:** поле `datetime` у відповіді записів приходить з offset `+03:00` **незалежно від сезону** — для візиту 06.01.2026 (Київ тоді у `+02:00`) це давало показ на годину раніше, ніж було насправді. Час візиту тому читається з наївного поля `date` (справжній час на годиннику салону) з явною локалізацією в Київ. Покрито тестом `test_winter_visit_keeps_salon_wall_clock_time`.
- ✅ Юніт-тести `services/test_visit_history.py`: фільтрація (майбутні/видалені/«не прийшов»/без позначки), сума кількох послуг у візиті, злиття філій із сортуванням, ізоляція збою однієї філії, помилка коли не відповіла жодна, зимовий час

**Лишилось до ✅:**
- ⬜ Живий тест у проді: реальний клієнт тапає `✂️ Історія` і бачить свої візити (локально перевірено лише збір і рендеринг даних, не сам тап у Telegram)
- ⬜ Перевірити клієнта з візитами більш ніж в одній філії — злиття перевірене юніт-тестом, але не на живих даних (у тестового клієнта всі 12 візитів в одній філії)

**Критерій готовності:** клієнт бачить свої минулі візити з усіх філій, найновіші першими, включно з візитами до появи бота.

---

### ⬜ Фаза 6 — Бонусна програма

**Основний варіант — Loyalty API Altegio:**
- `🎁 Бонуси`: баланс з `get_loyalty_balance()` + умови програми (текст у налаштуваннях).
- Нарахування/списання відбуваються в Altegio при оплаті — бот лише показує.
- Пуші про операції: якщо Altegio шле вебхуки фінансових операцій — з них; інакше cron порівнює баланс із збереженим і шле "➕ Нараховано 80 бонусів. Баланс: 230".

**Запасний варіант** (якщо салон не використовує loyalty Altegio або API його не віддає): власні таблиці `bonus_accounts`/`bonus_ops`, нарахування X% після візиту, ручні операції адміна через `/admin`.

**Критерій готовності:** клієнт бачить актуальний баланс; про зміну балансу приходить пуш.

---

### 🟨 Фаза 7 — Персональні пропозиції та автоматизація

Нові типи в `notifications` + cron:

- ✅ **День народження улюбленця** (реалізовано 2026-08-04, поки що без знижки): щоденна перевірка `pets.birth_date` (`db.get_pets_with_birth_date()` + збіг місяця/дня в Python — PostgREST не фільтрує по даті без року) → пряма відправка (той самий підхід, що `vaccine_sync.py` — без проміжного рядка в `notifications`) тексту "🎂 З днем народження, {ім'я}! Бажаємо здоров'я та веселих пригод! 🐾". Знижка з оригінального опису фази (20% цього тижня) свідомо відкладена — рішення про розмір/умови ще не прийняте; коли буде, дописати текст і логіку окремо. `services/birthday.py`, `BIRTHDAY_HOUR=9` у `services/scheduler.py`.
- ✅ **Ревакцинація**: ~~`rabies_vaccine_date + 1 рік − 2 тижні`~~ дату вакцинації бот не збирає (Фаза 1) — вона в коментарі картки клієнта Altegio. Нагадування клієнтам у Telegram реалізовано в окремій **Фазі 10** (щоденна синхронізація дат з коментарів Altegio → Supabase → сповіщення за 7 днів) — деталі там, тут лишається лише посилання.
- ✅ **"Маємо вікно завтра"** (реалізовано 2026-07-31): щоденна перевірка (`REBOOK_PROMO_HOUR=9` у `services/scheduler.py`) клієнтів з простроченим (6+ тижнів, `REBOOK_DEFAULT_WEEKS`) останнім активним записом (`db.get_active_tracked_records_with_ends_at()`) → якщо в Altegio на завтра є вільні місця по локації клієнта (`services/altegio.py::get_available_dates`) — промо з кнопками «📅 Записатись», «🔔 Нагадати наступного тижня», «❌ Скасувати нагадування». Anti-spam двошаровий: кулдаун `last_promo_at` (не частіше раз на 7 днів, природно дає "раз на тиждень" без прив'язки до дня тижня) + явний дісміс `clients.rebook_promo_dismissed_record_id` (мігація `008_phase7_rebook_promo.sql`, виконана на живій БД), який глушить конкретно цей прострочений візит; після нового візиту клієнта лічильник стартує заново автоматично. «Нагадати наступного тижня» і мовчазна відсутність реакції дають однаковий ефект (спробувати знову за тиждень). Per-client try/except у `services/rebook_promo.py` і в дісміс-колбеку `handlers/rebook_promo.py` — збій одного клієнта/запису не блокує решту й не губить прогрес батчу. `services/rebook_promo.py`, `handlers/rebook_promo.py`. Живий тест: реальний Altegio (підтверджена доступність слоту) + реальний Supabase (підтверджена доставка повідомлення й запис `last_promo_at`).
- **Поради від грумера** (косметика, частота догляду) — це рекомендації Фази 5, окремої механіки не треба.

**Критерій готовності:** тригери спрацьовують на тестових даних, anti-spam працює.

---

### 🟨 Фаза 8 — AI-помічник 2.0

1. ✅ **Контекст у промпт** (реалізовано 2026-08-20) — деталі нижче.
2. ⬜ **Function calling (Groq tool use)**: інструменти `get_free_slots(location, date)`, `get_price(service)`, `start_booking()` — AI відповідає "завтра є вікно о 15:00, записати?" і кнопкою веде у флоу запису.
3. ⬜ **Корисні поради**: розширити `SYSTEM_PROMPT` базою порад (догляд між грумінгами, розчісування, купання, сезонні) + застереження "не заміна ветеринару" для health-питань.
4. ⬜ Історію чату — в `chat_messages` (БД) замість in-memory, обрізати до ~20 повідомлень.

**Критерій готовності:** AI називає реальні ціни/адреси/слоти з Altegio і не вигадує.

**Зроблено — пункт 1 (спека: `docs/superpowers/specs/2026-08-19-ai-grounded-context-design.md`, план: `docs/superpowers/plans/2026-08-20-ai-grounded-context.md`):**
- ✅ `services/ai_context.py` — живий контекст під конкретного улюбленця: каталог послуг філії клієнта (`altegio.get_services`, кеш 1 год), назви категорій (`get_service_categories`, той самий кеш), адреси філій (`get_company`, кеш 1 доба), добір послуг за породою й вагою тим самим `handlers/booking.match_services_by_breed` і тією самою `format_price`, що і флоу запису. Незареєстрований клієнт (або той, у кого ще нема улюбленців) бачить лише діапазон «від … до …» і запрошення заповнити анкету.
  - **Ціна однієї послуги в чаті й при записі розійтись не може** (одне джерело, одне форматування), але **перелік** показаних послуг відрізняється навмисно: при записі клієнт спершу обирає категорію, і матчер працює в її межах, а в чаті такого кроку немає, тож добір іде по всьому каталогу. Тому рядок прайсу в чаті обов'язково містить категорію: у живому каталозі 104 назви з 327 повторюються між категоріями (категорія кодує тип послуги × рівень грумера), і без неї модель отримувала б однакові назви з різними цінами. Якщо запит категорій не вдався — однакові назви згортаються в один рядок, щоб суперечливих цін у промпті не було в жодному разі.
  - **Інваріант: текст клієнта не потрапляє в промпт.** Порода, ім'я улюбленця, алергії, нотатки — не підставляються; порода використовується **лише як ключ пошуку** по каталогу, а в блок даних іде назва послуги з Altegio і числова вага. Це знімає найдешевший вектор prompt injection: клієнт сам заповнює ці поля в анкеті. Покрито тестом, який пише в поля улюбленця текст-«інструкцію» і перевіряє, що її немає у блоці.
  - Назви послуг з Altegio проганяються через `_clean()` — зокрема вирізається маркер `===`, щоб рядок каталогу не міг підробити межу блоку даних і «закрити» його раніше.
  - Деградація без падінь: Altegio недоступний → віддається прострочений кеш, а якщо кешу нема — контекст без цін; Supabase недоступний → загальний контекст замість персонального; будь-яка інша помилка → порожній контекст і звичайна AI-відповідь без цін.
- ✅ `services/ai_guard.py` — детерміновані запобіжники **на виході** моделі (промпту не довіряємо):
  - Суми у відповіді звіряються з множиною сум, які реально були в контексті (`unknown_amounts`); нормалізація зводить «1 300 грн», «1300грн», «₴1300», «UAH 1300» до одного числа, а всі різновиди діапазону («900–1400 грн», «900/1400 грн», «від 900 до 1400 грн») — до однієї форми, тож нижня межа не губиться. Знайшлась вигадана сума → відповідь моделі **підміняється** детермінованим списком реальних цін (`price_fallback`), клієнт хибної ціни не бачить.
  - Інваріант тримає round-trip тест в обидва боки: множина сум, видима перевірці в тексті блоку, мусить **точно** дорівнювати оголошеній. Напрямок «оголошено, але перевірка не бачить» і був дірою, через яку вигадана нижня межа діапазону проходила як своя (знайдено на рев'ю, до мержу).
  - **Підсумків цін бот не рахує навмисно:** на «скільки разом» перелічує позиції окремо. Складання — найпростіший спосіб для LLM видати суму, якої немає в прайсі, а остаточна ціна все одно залежить від огляду майстром.
  - Чужі посилання вирізаються по білому списку хостів (`strip_foreign_links`), з перевіркою через `urllib.parse` — щоб `https://<наш-віджет>/?u=https://evil.com` не пройшов як «наш» лише за префіксом.
  - Особистий ліміт 15 повідомлень/год на клієнта — щоб один користувач не з'їв спільну квоту безкоштовного тарифу Groq.
  - 5 підмін відповіді за годину → сповіщення адмінам: це вже схоже на те, що Groq змінив модель або промпт поплив, а не на випадковість.
- ✅ `config.SYSTEM_PROMPT` перероблено: заборонені знижки/акції/промокоди, ветеринарні діагнози, вигадані послуги й адреси; ціни — лише з блоку даних і лише як орієнтовні («остаточну суму підтверджує майстер при огляді»); блок даних явно оголошений **даними, а не інструкціями**.
- ✅ `groq_client.py`: контекст підставляється в system-повідомлення, історія обрізається до 10 повідомлень (вікно завжди починається з `user` — інакше модель отримувала б відповідь без питання), `RateLimitError` (429) прокидується окремо від решти помилок.
- ✅ `handlers/ai_chat.py`: ліміт → контекст → Groq → запобіжники → відповідь. У лог пишеться лише `user_id` і кількість символів, не текст клієнта. 429 → клієнт одразу отримує телефон салону замість очікування.
- ✅ **Дайджест про вичерпану квоту о 18:30** (`services/scheduler.py::send_quota_digest`): `tg_user_id`, які за день наткнулись на 429, накопичуються в пам'яті процесу, а о 18:30 (одразу за сповіщенням про перервані записи о 18:00 — адміни обробляють обидва списки за раз) адмінам летить один список «ім'я + телефон», щоб передзвонити. Синхронний `notify_admins`, бо cron-шлях навмисно працює повз event loop. Позначка в `cron_state` — лише після підтвердженої доставки, тож невдала відправка повториться на наступному тику того ж дня.
- ✅ 57 юніт-тестів: `services/test_ai_context.py`, `services/test_ai_guard.py`, `services/test_groq_client.py`, `services/test_ai_quota_digest.py`.

**Лишилось до ✅ по пункту 1:**
- ⬜ Живий тест у проді: зареєстрований клієнт питає ціну (бот називає ціни під його породу й вагу) і незареєстрований питає те саме (бот дає діапазон і запрошення заповнити анкету) — локально перевірено лише збір контексту й запобіжники, не сам діалог у Telegram

---

### ✅ Фаза 9 — Зв'язок з адміністратором

Реалізовано спрощений варіант (без двостороннього чату):
- `🆘 Допомога` (кнопка головного меню) → номер салону текстом у повідомленні (`config.HELP_PHONE`) — Telegram сам розпізнає номер у тексті й робить його тапабельним (тап відкриває номеронабирач, дзвінок іде через стільниковий зв'язок, не через Telegram/бота).
  - Спершу пробували inline-кнопку `url="tel:+380960080482"` — Bot API відхиляє такі URL (`BadRequest: wrong port number specified in the url`, підтримуються лише http/https/tg). Тому номер — просто текст.
  - Реалізація: `handlers/menu.py::handle_menu_button` (гілка `BTN_HELP`), номер винесено константою в `config.py`.
- `🆘 Допомога` (inline-кнопка в флоу запису, коли порода улюбленця не знайдена серед послуг обраного рівня) → та сама текстова відповідь із номером. Реалізація: `handlers/booking.py` (callback `bk_contact_admin`).

Пересилання тексту в адмін-топік і двосторонній reply-діалог клієнт↔адмін — не реалізовано, лишається як можливе розширення.

**Додатково: сповіщення про незавершений запис.** Клієнт натиснув «📅 Записатись» (або «Повторити останній запис»), але не оформив запис до кінця дня → о 18:00 (Київ) адмінам летить повідомлення з ім'ям і телефоном клієнта, щоб передзвонити й з'ясувати, чи не виникла проблема. Той самий патерн, що `form_incomplete` (Фаза 1): планується в момент старту флоу, cron о 18:00 перевіряє, чи не з'явився активний `tracked_record`, і шле лише якщо запис так і не оформили; дедуп — не більше одного pending-сповіщення цього типу на клієнта одночасно.
- Міграція: `db/migrations/003_phase9_booking_incomplete.sql` (додає `booking_incomplete` у `notifications.type`).
- `db/client.py::has_pending_notification`, `has_tracked_record_since`.
- `services/notifications.py::schedule_booking_incomplete` (виклик з `handlers/booking.py::_start` при `mode="book"` і з `start_from_pet_and_service`), `services/scheduler.py::_handle_booking_incomplete`.
- `💰 Дізнатись вартість` (mode="price") цей тригер не запускає — клієнт лише цікавився ціною, а не записувався.

---

### ✅ Фаза 10 — Дати вакцинації: Altegio-коментарі → Supabase → нагадування клієнтам

Адміністратори вручну записують дату вакцинації в коментар картки клієнта в Altegio.
Бот щоранку переносить ці дати в Supabase і за тиждень до закінчення нагадує клієнту в Telegram.

**Прийняті рішення (2026-07-23, уточнено того ж дня):**
- **Формат запису в коментарі:** будь-яка дата (`ДД.ММ.РРРР`/`ДД.ММ.РР`, роздільники `.`/`/`/`-`) — без ключового слова, перша знайдена в **усьому тексті коментаря** (без прив'язки до маркера `altegio_sync.BOT_BLOCK_MARKER`). Адміни свідомо не пишуть у це поле жодних інших дат, крім вакцинації — тож звужувати пошук до частини коментаря не потрібно.
- **Що означає дата:** дата закінчення дії вакцини (нагадування = `дата − 7 днів`, без розрахунку `+1 рік`).
- **Клієнт чи улюбленець:** одна дата на клієнта — нова колонка `clients.vaccine_due_date` (не `pets.rabies_vaccine_date` — той лишається про запас, як і раніше).
- **Охоплення:** синхронізація/нагадування можливі лише для клієнтів, які вже є в Supabase (`altegio_client_id` заповнений, тобто клієнт хоч раз проходив `/start`). Клієнт, відомий лише в Altegio, поза охопленням, доки сам не зареєструється в боті — Telegram не дозволяє боту писати першим.
- **Час запуску:** 10:00 Київ (не ввечері) — щоб клієнт бачив нагадування вранці, а не вночі.
- **Механізм нагадування:** без проміжного рядка в `notifications` — `sync_vaccine_dates()` рахує різницю в днях напряму під час щоденного запуску і шле повідомлення відразу, якщо лишилось рівно 7 днів; дедуп — колонка-флаг `clients.vaccine_notified_due_date` (порівнюється з поточною `vaccine_due_date`, оновлюється після успішної відправки; нове нагадування можливе лише коли адмін впише нову дату).

**Зроблено (код у master):**
- ✅ `db/migrations/004_phase10_vaccine_due_date.sql` — `clients.vaccine_due_date` + таблиця `cron_state` (позначка останнього запуску щоденних задач, щоб диспетчер не ганяв синхронізацію на кожному 10-хвилинному тику)
- ✅ `db/migrations/005_phase10_vaccine_notified_flag.sql` — `clients.vaccine_notified_due_date` (флаг дедупу нагадувань)
- ✅ `services/vaccine_sync.py` — `sync_vaccine_dates()`: для кожного клієнта з `altegio_client_id`/`altegio_company_id` читає `altegio.get_client()`, парсить першу дату по всьому тексту коментаря; якщо дата змінилась — оновлює `vaccine_due_date`; якщо лишилось ≤7 днів і нагадування на цю дату ще не надсилалось — шле напряму через `notifications.send_telegram_message()` і виставляє `vaccine_notified_due_date`
  - **Виправлено (2026-07-24):** початково умова була "рівно 7 днів" (`==`) — живий тест (2026-07-23, тестовому клієнту дописано дату 30.07) показав вразливість: код задеплоєно ввечері 23.07, вже після вікна `VACCINE_SYNC_HOUR=10:00`, тож перший запуск синку відбувся аж 24.07, коли лишилось уже 6 днів — вікно "рівно 7" пройшло непоміченим, клієнт не отримав би нагадування взагалі (флаг дедупу не дає другого шансу на ту саму дату). Замінено на `≤7` (`due_date - today > 7 → skip`): синк наздоганяє пропущений день на наступному запуску; дедуп-флаг лишається без змін і так само захищає від повторної відправки того ж дня.
- ✅ `db/client.py`: `get_clients_with_altegio_link()`, `get_cron_last_run()`/`set_cron_last_run()`
- ✅ `services/scheduler.py`: `_run_daily_tasks()` — перший виклик `/cron` після 10:00 (Київ, `VACCINE_SYNC_HOUR`), для якого сьогодні ще не було запуску (`cron_state`), викликає `sync_vaccine_dates()`; окремого обробника в `_HANDLERS` немає — відправка йде напряму з `vaccine_sync.py`

**Лишилось до ✅:**
- ✅ `db/migrations/004_phase10_vaccine_due_date.sql` виконано в Supabase
- ✅ `db/migrations/005_phase10_vaccine_notified_flag.sql` виконано в Supabase
- ✅ Живий тест (2026-07-23, клієнт András, дата 30.07): виявив баг вікна "рівно 7" (див. вище) — нагадування не прийшло. Фікс задеплоєно 2026-07-24; підтверджено живим тестом 2026-07-28 — нагадування о 10:00 (Київ) про вакцинацію, що скоро закінчується, реально надійшло клієнту в Telegram

**Критерій готовності:** адмін пише дату в коментар клієнта в Altegio → наступного ранку дата в Supabase → за 7 днів до дати клієнт отримує одне нагадування в Telegram.

---

### 🟨 Фаза 11 — «Найближчі віконця» (запис через конкретного грумера, без віджету)

Після редизайну Фази 2 (2026-07-27) `📅 Записатись` веде на зовнішній Altegio-віджет, де клієнт сам обирає філію/грумера/послугу/час. Ця фаза повертає **окрему**, паралельну кнопку меню для клієнтів, які хочуть швидко побачити найближчий вільний час у конкретного грумера, не проходячи весь віджет.

**Флоу (нова кнопка меню, напр. `🔜 Найближчі віконця`):**
1. Улюбленець (якщо один — пропускається, як і в поточних флоу).
2. Локація — `config.ALTEGIO_LOCATIONS` (та сама механіка, що була у видаленому `_location_keyboard`/`_show_location_card` до редизайну Фази 2 — код можна відновити з git-історії комітом до 2026-07-27).
3. Грумер — список майстрів обраної локації (`altegio.get_staff(company_id)`), карткою `name` (рівень уже в самій назві: `"PRO грумер Саша"`, `"ТОП грумер Тимур"` — окремо показувати не треба) **+ кнопка `🎲 Будь-який майстер`** (як і в звичайному флоу запису до редизайну — `staff_id=0`, резолв на конкретного майстра відкладається до підтвердження через `find_available_staff_for_slot()`).
4. Послуги обраного майстра, що підходять під породу улюбленця — рівень майстра визначається з тексту `name` (`_category_type_key`/логіка рівня, вже є в `handlers/booking.py`), далі звичайний breed-fallback (`_match_services_by_breed`) серед категорій цього рівня:
   - **Знайдено на рівні цього майстра** → показуємо послуги, йдемо далі.
   - **Не знайдено на рівні цього майстра, але є на іншому рівні** (`_sibling_categories`, як у Фазі 2) — те саме повідомлення, що й зараз: «є в майстра іншого рівня» + пропозиція повернутись до кроку 3 і обрати майстра відповідного рівня (список майстрів можна одразу звузити до цього рівня).
   - **Не знайдено на жодному рівні** — той самий генеральний фолбек, що й у `💰 Дізнатись вартість` (`_generic_breed_services`/`_show_generic_fallback`): кнопки «Інші породи» за вагою + повний прайс + `🆘 Допомога`.
   - Якщо обрано «Будь-який майстер» — рівень не обмежуємо, показуємо breed-fallback по всіх рівнях одразу (як зараз у `💰 Дізнатись вартість`).
5. Для кожної кандидат-послуги тривалість (`seance_length`) відома лише зі слоту `book_times` (не з `book_services`/`book_staff`, з'ясовано в Фазі 2) — тож найближчі вільні віконця будуються послідовним обходом: `get_available_dates(company_id, staff_id, service_ids=[service_id])` → для кожної найближчої дати `get_available_times(company_id, staff_id, date, service_ids=[service_id])`, поки не назбирається достатньо слотів (напр. 5–8) або не скінчиться розумний горизонт (напр. 14 днів). При «Будь-якому майстрі» `staff_id=0` — так само, як і в звичайному флоу дат/часів.
6. Список найближчих віконець (дата + час) — кожен пункт як inline-кнопка вибору; в кінці списку — `❌ Скасувати`. Вибір слоту одразу веде на підтвердження: якщо майстер відомий — без `find_available_staff_for_slot()` (як у гілці «відомий майстер» в `_confirm_booking`/`_confirm_reschedule`); якщо «Будь-який майстер» — резолв через `find_available_staff_for_slot()`, як і зараз → `create_record()` → `tracked_records` (той самий патерн, що й решта флоу запису).

**Що реюзати (не писати заново):**
- `handlers/my_bookings.py::_ask_reschedule_date/_ask_reschedule_time/_confirm_reschedule` — найближчий існуючий приклад «конкретний `staff_id` → дати → часи» (переніс запису вже так працює).
- `handlers/booking.py::_match_services_by_breed`, `_sibling_categories`, `_generic_breed_services`/`_show_generic_fallback`, `_ask_category`/`_ask_service` — уся breed-fallback логіка з рівнями майстрів і генеральним фолбеком, лишається як є, без змін.
- `services/altegio.py::get_staff/get_service_categories/get_available_dates/get_available_times/find_available_staff_for_slot/create_record` — без нових ендпоїнтів Altegio.
- Стан флоу — в `context.user_data`, як і в `booking.py`/`my_bookings.py` (не персистентний, ConversationHandler не потрібен).

**Критерій готовності:** клієнт обирає локацію → грумера → бачить лише ті послуги, що цей грумер реально надає для породи його улюбленця → список найближчих вільних віконець саме в цього грумера → тап по віконцю створює запис в Altegio без додаткового резолву майстра.

**Зроблено (код у master):**
- ✅ `handlers/booking.py`: кілька приватних хелперів промувано в публічні для перевикористання з нового модуля (без зміни поведінки) — `format_price`, `location_name`, `slim_service`, `match_services_by_breed`, `generic_breed_services`, `registered_client_pets`
- ✅ `handlers/nearest_slots.py` (новий модуль, `ns_...` callback data) — повний флоу: улюбленець → локація → грумер (+ 🎲 Будь-який майстер) → послуги з фільтром по рівню майстра (рівень визначається з тексту `name`/назви категорії) з фолбеками (сусідній рівень → «🔁 Обрати іншого майстра», інакше генеральний «Інші породи»/повний прайс/🆘) → пошук найближчих вільних віконець послідовним обходом `get_available_dates`/`get_available_times` → підтвердження → `create_record()` → `tracked_records`
  - На підтвердженні — живий перепит Altegio перед `create_record()` для обох гілок (і «конкретний майстер» через `get_available_times()`, і «будь-який майстер» через `find_available_staff_for_slot()`): якщо слот хтось зайняв, поки клієнт дивився список, — чітке повідомлення "цей час щойно зайняли" замість помилки чи тиші
  - `notifications.schedule_booking_incomplete(client_id)` викликається одразу на вході у флоу (кнопка «🔜 Найближчі віконця»), як і в `booking.py::book_start` — якщо клієнт почав пошук, але так і не завершив запис до 18:00, адмінам летить те саме сповіщення (Фаза 9); дедуп і перевірка завершення (`has_tracked_record_since`) — спільні для всіх флоу запису, окремого підключення не потребували
- ✅ `handlers/menu.py`: нова кнопка `BTN_NEAREST = "🔜 Найближчі віконця"`, окремим рядком меню
- ✅ `handlers/setup.py`: реєстрація `nearest_slots.start` (кнопка) і `nearest_slots.handle_callback` (`^ns_`)

**Лишилось до ✅:**
- ⬜ Живий тест: перевірити, що визначення рівня грумера з тексту `name` (`"топ"`/`"pro"`/база) справді збігається з рівнем у назвах категорій на реальних даних Altegio — це евристика, не перевірена на проді
- ⬜ Живий тест повного флоу (усі 3 локації, «🎲 Будь-який майстер», фолбек на сусідній рівень, генеральний фолбек)

---

## Порядок і залежності

```
Фаза 0 ✅ (фундамент + Altegio API + вебхуки)
 ├─→ Фаза 1 ✅ (реєстрація/улюбленці, клієнт → Altegio)
 │     └─→ Фаза 2 ✅ (запис через Altegio) ─→ Фаза 3 ✅ (управління записами)
 │           └─→ Фаза 4 (нагадування/оцінки)
 │                 └─→ Фаза 5 🟨 (історія візитів; фото/рекомендації майстра — не робимо)
 │                       ├─→ Фаза 6 (бонуси з Altegio Loyalty)
 │                       └─→ Фаза 7 (персональні пропозиції)
 ├─→ Фаза 8 (AI 2.0) — після Фази 2
 ├─→ Фаза 9 (зв'язок з адміном) — будь-коли
 ├─→ Фаза 10 (вакцинація: Altegio-коментарі → Supabase → нагадування) — після Фази 1
 └─→ Фаза 11 (найближчі віконця через конкретного грумера) — розширення Фази 2/3, будь-коли після них
```

**Рекомендований MVP-реліз = Фази 0–4.** Далі — по фазі за раз.

---

## Ризики та відкриті питання

| # | Питання | Що вирішити |
|---|---------|-------------|
| 1 | **Доступ до Altegio API** | Чи відкритий API на тарифі салону; отримати partner + user token, company_id. Це блокер Фази 0 — з'ясувати першим. |
| 2 | **Улюбленці в Altegio API** | Чи віддає API картки тварин. Якщо ні — тварини живуть у нашій БД, в Altegio дублюємо текстом у коментарях/custom fields. |
| 3 | **Loyalty API** | Чи користується салон бонусами Altegio і чи читається баланс по API. Якщо ні — запасний варіант у Фазі 6. |
| 4 | Cron на безкоштовному PythonAnywhere | Free tier = 1 щоденний scheduled task → нагадування "за 2–3 год" з нього неможливі. Рішення без витрат: cron-job.org б'є в `/cron` endpoint кожні 10–15 хв. |
| 5 | ~~Білий список PythonAnywhere (free)~~ ✅ | **Знято 2026-07-20:** `api.alteg.io` додано в білий список PythonAnywhere (підтримка відповіла того ж дня, запит подано 2026-07-19). Перевірено живим викликом з Bash-консолі — замість `403 ProxyError` повертається відповідь самого Altegio API. `*.supabase.co` і `api.groq.com` вже проходили раніше. Прямі Postgres-з'єднання (порт 5432) і надалі заблоковані завжди → тільки Supabase REST. |
| 6 | Правило 24 год | Узгодити з налаштуванням вікна онлайн-скасування в самому Altegio, щоб бот і сайт поводились однаково. |
| 7 | Вебхуки Altegio | Перевірити, які саме події приходять (records точно; фінансові операції — перевірити для бонус-пушів) і чи є підпис/автентифікація. |
| 8 | ~~Ціни за розміром~~ ✅ | **З'ясовано:** салон уже веде 327 окремих послуг по породі+вазі в Altegio (напр. "Йоркширський тер'єр до 4 кг" — 1300₴). Бот просто показує список `book_services`, свою логіку категорій вигадувати не треба. |
| 9 | Сон Supabase | Free-проєкт «засинає» після 7 днів без запитів. Живий бот робить запити щодня, а cron кожні 10 хв читає `notifications` — засинання не буде. |
| 10 | Бекапи | ✅ Закрито 2026-08-18: `services/backup.py` щотижня (субота, підхват у неділю) надсилає повний JSON-дамп усіх таблиць в особистий чат власника в Telegram. Від початкового «експорту на диск PythonAnywhere» свідомо відмовились: копія на тій самій машині, що й бот, не переживає проблем з акаунтом, тобто не покриває половину сценаріїв, для яких бекап і потрібен. Скрипт відновлення навмисно не писався — перевірити його можна лише на живій базі або на окремому тестовому проєкті Supabase, а неперевірений скрипт дає хибне відчуття безпеки. Спека: `docs/superpowers/specs/2026-08-18-supabase-backup-design.md`. |

---

## Чекліст оновлень існуючих файлів

- `requirements.txt`: + `supabase`, `requests` (або `httpx`)
- `config.py`: + `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `ALTEGIO_PARTNER_TOKEN`, `ALTEGIO_USER_TOKEN`, `ALTEGIO_COMPANY_IDS`, `ADMIN_GROUP_CHAT_ID`, `ADMIN_TOPIC_ID`, `CRON_SECRET`, `GOOGLE_MAPS_REVIEW_URL`
- `.env`: + нові секрети
- `webhook_bot.py`: нові handler-и, `/cron` і `/altegio/webhook` endpoints (файл лишається тонким)
- `bot.py`: ті самі handler-и для локального polling-тестування
- `CLAUDE.md`: оновлювати після кожної фази