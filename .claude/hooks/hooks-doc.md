https://code.claude.com/docs/en/hooks

"hooks": [
{
"type": "command", or "agent", or "prompt"
"command": "cd \"$CLAUDE_PROJECT_DIR\" && bun run format 2>/dev/null || true" - можна передати сюди команду яку ти передаєш в термінал для виконання і вона буде виконуватись клодом
}